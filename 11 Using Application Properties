Customizing the Spring appplication is through the property file -> applicatiom.properties
The 8080 port is the default property that is configured by default in the servlet.

We can change the application server and place in the properties and then we can configure it.

