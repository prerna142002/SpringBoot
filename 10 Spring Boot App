Starting a Spring Boot App
1. Spring Initializr : https://start.spring.io 
  We can create the application and download the zip file.
  
2. Spring Boot CLI :
   Install and run using java -jar command
   >> ./spring
   >> gives the help commands to use for the spring command framwork.
   >> run // runs the groovy scripts 
   >> nano app.groovy // Just need to create a Controller class no need to create the Application class.
      @RestController
      class AppCtrl {
        @RequestMapping("/")
        String app(){
          return "Hello World";
        }
      }
     >> ./spring run app.groovy
      
3. STS IDE



